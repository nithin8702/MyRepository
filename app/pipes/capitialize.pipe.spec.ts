import { CapitializePipe } from './capitialize.pipe';

describe('CapitializePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitializePipe();
    expect(pipe).toBeTruthy();
  });
});
